name: mock_api
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dio: ^5.9.0
  retrofit: ^4.6.0
  logger: ^2.6.0 
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  retrofit_generator: ^10.0.1
  build_runner: ^2.6.0
  json_serializable: ^6.10.0

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/json/response_locator.json
    - assets/json/jokes_ten.json